pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "zeeks-world"

include(":core")
include(":desktop")
include(":android")
